// Copy to secrets.h (gitignored) and fill in values.
// See firmware/cores3/README.md §Secrets

#pragma once

// Wi-Fi credentials
#define WIFI_SSID "LASSIKORAS5"
#define WIFI_PASS "kutagawa69"

// Backend server. Use the Studio's local IP (192.168.50.163) — the CoreS3
// is on the same LAN and can't resolve Tailscale hostnames.
#define BACKEND_HOST "192.168.50.163"
#define BACKEND_PORT 8000

// Device token — get one from the server:
//   walkie-agent cores3 token issue --device-name kids-room-1
#define DEVICE_TOKEN "REPLACE_ME"
