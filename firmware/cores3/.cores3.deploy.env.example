# Copy to .cores3.deploy.env (gitignored) and fill in values.
# deploy_to_cores3.sh sources this file.

# Mac Studio Tailscale hostname
STUDIO_HOST="jakubsikora@mac-studio-jakub.tail5d39b4.ts.net"

# Remote firmware root on the Studio
REMOTE_ROOT="/Users/jakubsikora/walkie-talkie/firmware/cores3"

# SSH password. Optional — if unset, deploy uses your SSH key.
SSHPASS=""
