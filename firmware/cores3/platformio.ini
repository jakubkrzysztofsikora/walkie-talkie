; PlatformIO Project Configuration File
;
;   Build options: build flags, source filter
;   Upload options: custom upload port, speed and extra flags
;   Library options: dependencies, extra library storages
;   Advanced options: extra scripting
;
; Please visit documentation for the other options and examples
; https://docs.platformio.org/page/projectconf.html

[env:m5stack-cores3]
platform = espressif32@6.10.0
board = m5stack-cores3
framework = arduino
monitor_speed = 115200
monitor_filters = esp32_exception_decoder
board_build.psram = enabled
board_build.flash_mode = qio
board_build.flash_freq = 80m
build_flags =
	-DCORE_DEBUG_LEVEL=4
	-DBOARD_HAS_PSRAM
	-DUI_ENGINE
	-std=gnu++17
	; UI_ENGINE makes the sprite back-buffer renderer the default (Phase 2). To
	; build the inline draw_ui() fallback instead, remove the -DUI_ENGINE line above.
lib_deps =
	m5stack/M5Unified@^0.2.4
	links2004/WebSockets@^2.5.0
	https://github.com/pschatzmann/arduino-audio-tools.git
	m5stack/M5GFX@^0.2.4
	bblanchon/ArduinoJson@^7.0.0
	sh123/esp32_opus@^1.0.3

[env:native]
platform = native
lib_compat_mode = off
lib_deps =
	walkie_ui_logic
build_flags =
	-DUNITY_INCLUDE_DOUBLE
	-std=gnu++17
	-I src
